# Visual references — Cherry Phan

Drop Cherry's real work screenshots into this folder. Fable consumes every image here as **primary visual direction** for the portfolio (see spec §4a). Any filenames are fine; captioning them below helps.

Suggested set (from what we have so far):

- `banner-*` — her personal banner: italic-serif name in a Figma selection-frame, halftone florals, headshot.
- `aurora-*` — the Aurora deck frames: coral→pink→peach gradients, serif titles, 3D product renders.
- `everloop-*` — Record / Reflect / Revisit app screens: pink→peach gradient, scrapbook reflection cards, "Words from yourself" envelope.
- `mugmood-*` — amber/gold journaling app: moka pot, stars, phone mockups.
- `tessera-*` — blue→lilac micro-moments app: phone mockups, soft light.
- `wally-*` — student wallet app + the Wally character (if available).

If this folder is empty at build time, Fable proceeds from the written descriptions in spec §4a and §10 and notes it in the changelog.
